{
  "name": "cryptofolio",
  "version": "0.1.0",
  "private": true,
  "description": "SaaS de gestion de portefeuille de cryptomonnaies — Next.js + NestJS + Prisma",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "format": "prettier --write \"apps/*/src/**/*.{ts,tsx,css}\"",
    "format:check": "prettier --check \"apps/*/src/**/*.{ts,tsx,css}\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "prettier": "^3.4.2"
  },
  "engines": {
    "node": ">=18.17.0"
  }
}
